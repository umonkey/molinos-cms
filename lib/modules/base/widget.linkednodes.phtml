<?php

if (!empty($documents)) {
  $output = '';

  foreach ($documents as $doc) {
    $link = l('node/'. $doc['id'], htmlspecialchars($doc['name']));
    $output .= html::em('li', array(
      'class' => 'node-'. $doc['class'],
      ), $link);
  }

  print html::em('ul', $output);
}
