<?php

if (!empty($document)) {
  $code = bebop_render_object('type', $document['class'], null, array(
    'document' => $document,
    'tags' => $tags,
    ));

  if (!empty($code))
    print mcms::html('div', array(
      'class' => 'node '. $document['class'],
      ), $code);
}
