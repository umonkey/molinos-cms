<?php

if (!empty($list)) {
  $names = array();

  if (!empty($document))
    $names[] = mcms_plain(l($document));
  if (!empty($section))
    $names[] = mcms_plain(l($section));

  print mcms::html('title', join(' — ', $names));
}
