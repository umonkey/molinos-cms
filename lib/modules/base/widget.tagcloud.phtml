<?php

if (!empty($tags)) {
  print '<ul class=\'tagcloud\'>';

  foreach ($tags as $t) {
    print mcms::html('li', array(
      'style' => 'font-size:'. (100 + $t['percent']) .'%',
      ), l($t['id'], $t['name'])) ."\n";
  }

  print '</ul>';
}
