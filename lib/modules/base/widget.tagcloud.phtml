<?php

if (!empty($tags)) {
  print '<ul class=\'tagcloud\'>';

  foreach ($tags as $t) {
    $link = html::em('a', array(
      'href' => $t['__link'],
      ), $t['name']);

    print html::em('li', array(
      'style' => 'font-size:'. (100 + $t['percent']) .'%',
      ), $link) ."\n";
  }

  print '</ul>';
}
